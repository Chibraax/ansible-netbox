---

site: WordWilde

python3_interpreter: /mnt/home_encrypted/wbruce/dev/python/package_virtual/.envv/bin/python3

tenants:
  - netbox.netbox.netbox_tenant:
    netbox_url: "{{ netbox_url }}"
    netbox_token: "{{ netbox_token }}"
    data:
      name: Retard_tenant
      slug: my-tenant2
      description: Description of the tenant
    state: present
  - netbox.netbox.netbox_tenant:
    netbox_url: "{{ netbox_url }}"
    netbox_token: "{{ netbox_token }}"
    data:
      name: Salut
      slug: my-tenant
      description: Description of the tenant
    state: present
  - netbox.netbox.netbox_tenant:
    netbox_url: "{{ netbox_url }}"
    netbox_token: "{{ netbox_token }}"
    data:
      name: StudyP
      slug: my-tenant
      description: Description of the tenant
    state: present

all_vm:
  - netbox.netbox.netbox_virtual_machine:
    netbox_url: "{{ netbox_url }}"
    netbox_token: "{{ netbox_token }}"
    data:
      name: Test Virtual Machine
      site: "{{ site }}"
    state: present

netbox_url: http://localhost:8000
netbox_token: nbt_BOUKpSzmKnFJ.vlDgaHf7lmGdxHxO2Fny8srSdFk95QN4VOCAXxCD
